dataset: lorenz

window:
  length: 128
  stride: 32

split:
  train: 0.8
  val: 0.1
  test: 0.1
  seed: 42

scaler:
  type: standard

graph:
  type: lorenz
  nodes: [x, y, z]
  edge_type: fully_connected
  coordinate_type: state